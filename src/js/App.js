import React, { Component } from 'react';
import PlayerSearchBar from '../js/Containers/PlayerSearchBar';

export default class App extends Component {
    render() {
      return (
        <PlayerSearchBar />
      );
    }
}